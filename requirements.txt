# ============================================================
# mcu-rag-qa-v2 — P1 薄切片依赖
# ============================================================
# 安装: pip install -r requirements.txt
# 注意: P1 只列薄切片必需的包，P2/P3 按需追加

# --- 配置 ---
pydantic-settings>=2.0.0

# --- 索引 ---
chromadb>=1.5.0

# --- PDF 提取 ---
pypdf>=3.0.0

# --- LLM 调用 ---
openai>=1.0.0

# --- 向量化 ---
sentence-transformers>=2.2.0

# --- P2: 服务化 ---
fastapi>=0.100.0
uvicorn>=0.20.0

# --- P2: 混合检索 ---
rank-bm25>=0.2.0
jieba>=0.42.0

# --- P3: 异步任务 + 缓存 ---
celery[redis]>=5.4.0
redis>=5.0.0

# --- P3: 多来源数据 ---
python-docx>=1.1.0  # Word 文档读取

# --- P3: 可观测性 ---
prometheus-client>=0.20.0

# --- HTTP 请求（服务预热、健康检查等）---
requests>=2.28.0

# --- P4: 关系型数据库（SQLAlchemy + SQLite/PostgreSQL） ---
sqlalchemy>=2.0.0
alembic>=1.13.0  # 数据库迁移