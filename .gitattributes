# 换行符统一：仓库内一律存 LF，避免 Windows 开发 + Linux CI 混用时的 CRLF 干扰
# （首次提交时 git 对上百个文件报了 "LF will be replaced by CRLF" 警告）
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.db binary
*.bin binary
*.gguf binary
