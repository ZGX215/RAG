# ============================================================
# 开发 / CI 专用依赖
#
# 与 requirements.txt 分开：运行时镜像不需要这些，避免把它们打进生产镜像。
# 安装：pip install -r requirements.txt -r requirements-dev.txt
# ============================================================

# --- 测试 ---
pytest>=8.0.0
pytest-cov>=5.0.0
# tests/conftest.py 用它生成临时 PDF 夹具；运行时不需要
# （注意：包名是 fpdf2，但 import 名是 fpdf）
fpdf2>=2.7.0

# --- 静态检查 ---
ruff>=0.6.0
